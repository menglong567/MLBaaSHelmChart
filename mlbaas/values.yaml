# Default values for mlbaas.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: 127.0.0.1:5000/multichain-master:1.1.0
  pullPolicy: IfNotPresent

chainName: DemoChain

masterNodeName: demobaasmasternode #should be lower case
masterNodeNamespace: demobaasmasternode-namespace #should be lower case
masterNodeNodeportnetworkPort: 31100
masterNodeNodeportrpcPort: 31101
masterNodeMemoryRequest: 400
masterNodeCpuRequest: 400
masterNodeMemoryLimit: 400
masterNodeCpuLimit: 400